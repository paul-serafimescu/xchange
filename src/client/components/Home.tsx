import * as React from 'react';
import Searchbar from './Searchbar';

const Home: React.FC = () => {
  return (
    <React.Fragment>
      <Searchbar />
    </React.Fragment>
  );
};

export default Home;
